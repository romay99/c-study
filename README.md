# C 언어 학습 프로젝트

Java Spring 개발자를 위한 C 언어 학습용 프로젝트입니다.  
각 모듈에 **상세한 한글 주석**과 **Java와의 비교**가 포함되어 있습니다.

---

## 프로젝트 구조

```
c-study/
├── include/          # 헤더 파일 (.h)
│   ├── basics.h
│   ├── pointers.h
│   ├── struct.h
│   └── memory.h
├── src/
│   ├── main.c        # 메인 진입점
│   ├── basics/       # 기본 문법
│   ├── pointers/     # 포인터
│   ├── struct/       # 구조체
│   └── memory/       # 메모리 관리
├── build/            # 컴파일 산출물 (자동 생성)
├── bin/              # 실행 파일 (자동 생성)
├── Makefile
└── README.md
```

---

## 빌드 및 실행

### 요구 사항
- **GCC** 또는 **Clang** (C 컴파일러)

### Windows에서 컴파일러 설치
1. **MSYS2** (권장): https://www.msys2.org/
   - 설치 후 `pacman -S mingw-w64-ucrt-x86_64-gcc make` 실행
2. **MinGW-w64**: https://www.mingw-w64.org/
3. **Visual Studio**: C/C++ 개발 도구 포함된 버전

설치 후 터미널을 새로 열고 `gcc --version`으로 확인하세요.

### Windows (build.bat)
```cmd
build.bat
bin\c-study.exe
```

### Linux / macOS / WSL (Makefile)
```bash
make
make run
make clean
```

### gcc 직접 사용 (어디서든)
```bash
gcc -Wall -g -std=c11 -Iinclude -o bin/c-study src/main.c src/basics/basics.c src/pointers/pointers.c src/struct/struct.c src/memory/memory.c
```

---

## 학습 순서 추천

| 순서 | 모듈 | 설명 | Java와의 차이 |
|------|------|------|---------------|
| 1 | **basics** | 변수, 연산자, 제어문, 배열, 문자열 | String 없음, 배열 길이 고정 |
| 2 | **pointers** | 포인터, 주소, 역참조 | Java엔 포인터 없음 (핵심!) |
| 3 | **struct** | 구조체, 멤버 접근 | 클래스와 비슷하지만 메서드 없음 |
| 4 | **memory** | malloc, free, 동적 할당 | GC 없음, 수동 메모리 관리 |

---

## Java vs C 빠른 비교

| 항목 | Java | C |
|------|------|---|
| 진입점 | `main(String[] args)` | `main(int argc, char *argv[])` |
| 문자열 | `String` 클래스 | `char[]` 또는 `char*` |
| 배열 | 동적, `.length` | 고정 크기, 길이 별도 관리 |
| 메모리 | GC 자동 회수 | `malloc`/`free` 수동 관리 |
| 포인터 | 참조만 (연산 불가) | `*`, `&` 직접 사용 |
| OOP | 클래스, 상속, 다형성 | 구조체 + 함수 조합 |

---

## 참고 자료

- [C11 표준 개요](https://en.wikipedia.org/wiki/C11_(C_standard_revision))
- [GCC 문서](https://gcc.gnu.org/onlinedocs/)
- [C 프로그래밍 위키북](https://ko.wikibooks.org/wiki/C_%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%B0%8D)

---

화이팅! 🚀

# c-study
C 공부
